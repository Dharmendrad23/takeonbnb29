# Code Citations

## License: unknown
https://github.com/saikrishna1209/provast2/blob/ff23ba68bb39b9f881eca9cd7d368cf747417b0c/pages/auth/forgotpassword.js

```
Bhai, is chat interface se main **directly aapki file edit nahi kar sakta** — mere paas file-write access nahi hai, sirf code suggest karne ka tarika hai. Aapko manually copy-paste karna padega. Lekin main isko **sabse fast tareeka** bata deta hoon (30 second ka kaam hai):

### Fastest Method:

**Step 1:** Terminal me yeh command chalao — ye file khud khol degi:
```powershell
code "apps\web\src\pages\HostRegisterPage.jsx"
```

**Step 2:** File editor me khulte hi:
- `Ctrl + A` daba ke sab select karo
- `Ctrl + V` se replace karo (maine upar diya code copy kar lo)

**Step 3:** Save karo:
- `Ctrl + S`

Bas itna hi hai — 3 steps, 30 second.

---

### Agar aap chahte ho main "asaan" karu, to yeh bhi ek option hai:

Main aapko ek **PowerShell script** de sakta hoon jo khud file me content likh dega (aapko copy-paste bhi nahi karna padega, bas command chalana hoga):

```powershell
@'
import React, { useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import { Helmet } from "react-helmet";
import api from "@/lib/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  CardDescription,
} from "@/components/ui/card";
import { toast } from "sonner";
import { User, Mail, Lock, Loader2, KeyRound } from "lucide-react";

const HostRegisterPage = () => {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [step, setStep] = useState("email");
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [otp, setOtp] = useState("");
  const [password, setP
```


## License: unknown
https://github.com/Metis-IITGandhinagar/insiit-ui-react-native/blob/f5db483ba0087f9f46a573a7dd637f4c3dc3a8b3/screens/messForgotPasswordScreen.tsx

```
Bhai, is chat interface se main **directly aapki file edit nahi kar sakta** — mere paas file-write access nahi hai, sirf code suggest karne ka tarika hai. Aapko manually copy-paste karna padega. Lekin main isko **sabse fast tareeka** bata deta hoon (30 second ka kaam hai):

### Fastest Method:

**Step 1:** Terminal me yeh command chalao — ye file khud khol degi:
```powershell
code "apps\web\src\pages\HostRegisterPage.jsx"
```

**Step 2:** File editor me khulte hi:
- `Ctrl + A` daba ke sab select karo
- `Ctrl + V` se replace karo (maine upar diya code copy kar lo)

**Step 3:** Save karo:
- `Ctrl + S`

Bas itna hi hai — 3 steps, 30 second.

---

### Agar aap chahte ho main "asaan" karu, to yeh bhi ek option hai:

Main aapko ek **PowerShell script** de sakta hoon jo khud file me content likh dega (aapko copy-paste bhi nahi karna padega, bas command chalana hoga):

```powershell
@'
import React, { useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import { Helmet } from "react-helmet";
import api from "@/lib/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  CardDescription,
} from "@/components/ui/card";
import { toast } from "sonner";
import { User, Mail, Lock, Loader2, KeyRound } from "lucide-react";

const HostRegisterPage = () => {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [step, setStep] = useState("email");
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [otp, setOtp] = useState("");
  const [password, setP
```


## License: unknown
https://github.com/saikrishna1209/provast2/blob/ff23ba68bb39b9f881eca9cd7d368cf747417b0c/pages/auth/forgotpassword.js

```
Bhai, is chat interface se main **directly aapki file edit nahi kar sakta** — mere paas file-write access nahi hai, sirf code suggest karne ka tarika hai. Aapko manually copy-paste karna padega. Lekin main isko **sabse fast tareeka** bata deta hoon (30 second ka kaam hai):

### Fastest Method:

**Step 1:** Terminal me yeh command chalao — ye file khud khol degi:
```powershell
code "apps\web\src\pages\HostRegisterPage.jsx"
```

**Step 2:** File editor me khulte hi:
- `Ctrl + A` daba ke sab select karo
- `Ctrl + V` se replace karo (maine upar diya code copy kar lo)

**Step 3:** Save karo:
- `Ctrl + S`

Bas itna hi hai — 3 steps, 30 second.

---

### Agar aap chahte ho main "asaan" karu, to yeh bhi ek option hai:

Main aapko ek **PowerShell script** de sakta hoon jo khud file me content likh dega (aapko copy-paste bhi nahi karna padega, bas command chalana hoga):

```powershell
@'
import React, { useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import { Helmet } from "react-helmet";
import api from "@/lib/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  CardDescription,
} from "@/components/ui/card";
import { toast } from "sonner";
import { User, Mail, Lock, Loader2, KeyRound } from "lucide-react";

const HostRegisterPage = () => {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [step, setStep] = useState("email");
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [otp, setOtp] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");

  const handleSendOtp = async (e) => {
    e.preventDefault();
    
```


## License: unknown
https://github.com/Metis-IITGandhinagar/insiit-ui-react-native/blob/f5db483ba0087f9f46a573a7dd637f4c3dc3a8b3/screens/messForgotPasswordScreen.tsx

```
Bhai, is chat interface se main **directly aapki file edit nahi kar sakta** — mere paas file-write access nahi hai, sirf code suggest karne ka tarika hai. Aapko manually copy-paste karna padega. Lekin main isko **sabse fast tareeka** bata deta hoon (30 second ka kaam hai):

### Fastest Method:

**Step 1:** Terminal me yeh command chalao — ye file khud khol degi:
```powershell
code "apps\web\src\pages\HostRegisterPage.jsx"
```

**Step 2:** File editor me khulte hi:
- `Ctrl + A` daba ke sab select karo
- `Ctrl + V` se replace karo (maine upar diya code copy kar lo)

**Step 3:** Save karo:
- `Ctrl + S`

Bas itna hi hai — 3 steps, 30 second.

---

### Agar aap chahte ho main "asaan" karu, to yeh bhi ek option hai:

Main aapko ek **PowerShell script** de sakta hoon jo khud file me content likh dega (aapko copy-paste bhi nahi karna padega, bas command chalana hoga):

```powershell
@'
import React, { useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import { Helmet } from "react-helmet";
import api from "@/lib/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  CardDescription,
} from "@/components/ui/card";
import { toast } from "sonner";
import { User, Mail, Lock, Loader2, KeyRound } from "lucide-react";

const HostRegisterPage = () => {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [step, setStep] = useState("email");
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [otp, setOtp] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");

  const handleSendOtp = async (e) => {
    e.preventDefault();
    
```

